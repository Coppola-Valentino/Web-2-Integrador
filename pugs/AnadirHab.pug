doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Añadir Habitacion
    body(style="background: rgb(184, 253, 219);")
        include bar.pug
        +bar()
        block content
        div(style="max-width: 960px; margin: 0 auto; display: flex; justify-content: space-between; flex-direction: column; horizontal-align: center; padding-top: 120px;")
         h1 Añadir Habitacion al sistema
         form(action=`/Habit/anadir` method="POST" id="formi" style="display: flex; flex-direction: column; gap: 1px;")
           label(for="Ala") Ala de la Habitacion: 
            input(type="number" name="Ala" id="Ala" required)
           br 
           label(for="Tipo") Tipo de Habitacion: 
            select(name="Tipo" required)
             option(value="Urgencias") Urgencias
             option(value="Quimioterapia") Quimioterapia
             option(value="Cirujia") Cirujia
             option(value="Pediatria") Pediatria
             option(value="Traumatologia") Traumatologia
             option(value="Ginecologia") Ginecologia
             option(value="Neurologia") Neurologia
           br  
           button(class="btn btn-primary" type="submit") Guardar cambios
           br
           a.btn.btn-warning(href="/Habitaciones") Cancelar
              script.
                document.getElementById('formi').addEventListener('submit', function(event) {
                  const alaImp = document.getElementById('Ala');
                  if (alaImp.value.length > 4) {
                    alert('El Ala no puede tener más de 4 digitos.');
                    event.preventDefault();
                  }
                 });