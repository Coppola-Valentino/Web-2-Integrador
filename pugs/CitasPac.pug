//lista de citas medicas del paciente seleccionado (quizas limitar a que solo se vean las citas con el medico logueado o todas si es admin?(no hay tiempo pa eso))
doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Citas Medicas
    body(style="background: rgb(184, 253, 219);")
        include bar.pug
        +bar()
        block content
        div(style="max-width: 1500px; margin: 0 auto; display: flex; justify-content: space-between; flex-direction: column; horizontal-align: center; padding-top: 80px;")
         h1 Citas Medicas de: #{pac.Nombre}
         table 
          tr(style="border: 1px solid #ddd; background: rgb(170, 170, 170);")
           th Medico
           th Fecha 
           th Hora de inicio
           th Hora de Fin
           th Tipo de Cita
           th Acciones
           each cita in citas
            tr(style="border: 1px solid #ddd; background: rgb(255, 255, 255);")
             td #{cita.med.Especialidad} #{cita.med.Usuario}
             td #{cita.Fecha}
             td #{cita.HoraInicio}
             td #{cita.HoraFin}
             td #{cita.Tipo}
             td 
              a.btn.btn-info(href=`/inPac/${pac.IDPaciente}/EditCita/${cita.IDCita}`) Editar
        div(class="Container-fluid text-center justify-content-center")      
         div(class="col btn btn-link" style="Padding-bottom: 40px;")
          a.btn.btn-success(href=`/inPac/${pac.IDPaciente}/AnadirCita`) Programar Cita Medica
          br
         div(class="col btn btn-link" style="Padding-bottom: 40px;")
          a.btn.btn-warning(href=`/inPac/${pac.IDPaciente}/VistaMedica`) Volver